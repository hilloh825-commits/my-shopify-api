services:
  - type: web
    name: shopify-auth-api
    env: python
    region: singapore
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
      playwright install-deps chromium
    startCommand: python api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
